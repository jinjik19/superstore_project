sources:
  - name: stg_superstore
    database: superstore_db
    tables:
      - name: stg_orders
        columns:
          - name: row_id
            description: primary key
            tests:
              - unique
              - not_null
      - name: stg_returns
      - name: stg_people